import type { Settings } from '../types/settings'

export const guestModeSettings: Settings = {
  user: {
    name: 'Guest',
    stance: 'orthodox',
  },
  speech: {
    enabled: true,
    rate: 1,
    pitch: 1,
    volume: 1,
    voice: '',
  },
}
